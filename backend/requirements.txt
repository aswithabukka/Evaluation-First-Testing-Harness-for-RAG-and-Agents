# Web framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0

# Validation / settings
pydantic==2.10.0
pydantic-settings==2.6.0

# Task queue
celery==5.4.0
redis==5.2.0

# Evaluation libraries
ragas==0.2.6
deepeval==1.4.0
datasets==3.1.0

# LLM SDKs
openai==1.58.0
langchain==0.3.0
langchain-openai==0.2.0
llama-index==0.11.0

# PDF parsing
pypdf>=4.0,<7.0

# Utilities
python-dotenv==1.0.1
httpx==0.28.0
pyyaml==6.0.2
click==8.1.8

# Evaluator dependencies
# Hard dep — TrajectoryEvaluator and rule_evaluator's json_schema_valid rule
# both call into it. Graceful fallback exists but we want schema validation
# to work out of the box.
jsonschema>=4.20,<5

# ─── Optional evaluator deps (install only when enabling the layer) ────────
# Uncomment to enable:
#
# PII detection via Microsoft Presidio (SafetyEvaluator's Presidio layer).
# Adds ML-backed, multilingual PII detection on top of the regex fallback.
# presidio-analyzer>=2.2,<3
# presidio-anonymizer>=2.2,<3
#
# Llama Guard / ShieldGemma toxicity + jailbreak classification
# (SafetyEvaluator's guard_model layer). Requires a GPU for acceptable latency.
# transformers>=4.45,<5
# torch>=2.1,<3
#
# If you want to run the runner-side pytest suite inside the backend container:
# pytest>=8.0,<10
